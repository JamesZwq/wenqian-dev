// Auth tables are populated by Better-Auth in Phase 3 (Task 16) — that task
// generates `./auth.ts` and adds the re-export here.
//
// Each future feature spec adds its own `./<feature>.ts` file and re-exports
// from this file.

export {};
